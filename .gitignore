# Rendered devbox config and lock are environment-dependent
home/dot_local/share/devbox/global/default/devbox.json
home/dot_local/share/devbox/global/default/devbox.lock

# Local directories
.claude/
.firecrawl/

# Python tooling caches (pytest harness under tests/skills/)
__pycache__/
.pytest_cache/

# workmux Copilot CLI hooks (unused; auto-created by `workmux setup` if it detects Copilot CLI)
.github/hooks/

# Colima: track operator-edited templates, ignore runtime artefacts
# (default/, certs/, _lima/ are regenerated by colima itself on delete/start)
home/dot_config/colima/default/
home/dot_config/colima/certs/
home/dot_config/colima/_lima/
