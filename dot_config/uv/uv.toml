# uv configuration
# https://docs.astral.sh/uv/reference/settings/

# Use only uv-managed Python installations (never use system Python)
python-preference = "only-managed"

# Avoid newly published packages until the release has aged.
exclude-newer = "7 days"
